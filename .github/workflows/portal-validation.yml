name: Validate portal

on:
  pull_request:
    paths:
      - 'portal/**'
      - '.node-version'
      - '.github/workflows/portal-validation.yml'
      - '.github/workflows/deploy-portal.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  portal:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        base_path: ['./', '/zohelo-data/']
    env:
      DUCK_UI_BASEPATH: ${{ matrix.base_path }}
    defaults:
      run:
        working-directory: portal
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.node-version'
          cache: npm
          cache-dependency-path: portal/package-lock.json
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Check Google client-ID mapping and secret isolation
        run: node scripts/check-google-env.mjs
      - name: Lint
        run: npm run lint
      - name: Production build and type check
        run: npm run build
      - name: Unit and engine regression tests
        run: npm test
      - name: Install test browser
        run: npx --no-install playwright install --with-deps chromium
      - name: Browser regressions for Drive and public app information
        run: npm run test:e2e -- e2e/googleDrive.spec.ts e2e/publicInfo.spec.ts --retries=0
      - name: Retain failed browser traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: portal-browser-failure-${{ strategy.job-index }}
          path: portal/test-results/
          if-no-files-found: ignore
          retention-days: 7
