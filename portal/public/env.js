// Reserved runtime configuration surface. The supported static deployment keeps
// this empty; public build settings are supplied to Vite at build time. Loaded
// before the app bundle so window.env is always defined for inherited readers.
window.env = {};
